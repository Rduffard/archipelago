import { WorldPathStep } from '../../../../components/creator/IdentityStep/IdentityStep'

function RootsSection({ identity, onIdentityChange }) {
  return <WorldPathStep identity={identity} onIdentityChange={onIdentityChange} />
}

export default RootsSection
